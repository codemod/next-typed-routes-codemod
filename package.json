{
  "name": "my-pro-codemod",
  "version": "0.1.0",
  "description": "Transform legacy code patterns",
  "type": "module",
  "scripts": {
	"run:test": "pnpx codemod jssg test -l tsx ./scripts/codemod.ts --",
    "test:1": "pnpm run run:test './tests/project-1/src/app/(group)'",
    "test:2": "pnpm run run:test './tests/project-1/src/app/foo'",
    "test": "pnpm test:1 && pnpm test:2",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "^1.0.4",
    "@types/node": "^24.3.1",
    "typescript": "^5.8.3"
  }
}
